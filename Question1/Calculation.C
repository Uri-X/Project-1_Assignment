#include <stdio.h>

int main() {
    float radius = 5.0;
    float area = 3.14159 * radius * radius;
    printf("The area of a circle with radius %.2f is %.2f\n", radius, area);
    return 0;
}